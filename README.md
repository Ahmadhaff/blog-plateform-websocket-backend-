# WebSocket Server

Real-time WebSocket server for the MEAN Blog Platform.

## Installation

```bash
npm install
```

## Configuration

Create a `.env` file with MongoDB, Redis, RabbitMQ, and JWT configuration.

## Run

```bash
# Development
npm run dev

# Production
npm start
```

The server runs on port 3001 by default.
